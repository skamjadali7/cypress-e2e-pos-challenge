import React from 'react';

//Reusable Loader loader component or suspense component
const PageContentLoader: React.FC = () => {
  return <div className="page-content-loader">Loading...</div>;
};

export default PageContentLoader;
